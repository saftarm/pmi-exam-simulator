{
  "name": "testapi-fullstack",
  "version": "1.0.0",
  "description": "Root package.json to run React client and ASP.NET API simultaneously",
  "scripts": {
    "dev": "concurrently -n api,client -c blue,green \"dotnet run\" \"npm run dev --prefix reactclient\"",
    "dev:client": "npm run dev --prefix reactclient",
    "dev:api": "dotnet run",
    "install:client": "npm install --prefix reactclient"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

